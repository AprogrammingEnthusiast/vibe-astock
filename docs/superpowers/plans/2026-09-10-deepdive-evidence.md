# 深挖资料覆盖修复方案

**目标：** 将已核验的公开来源接入个股深挖，修正字段与分页错误，保留真实缺口；部署完成后由用户触发分析。

**方案：** 沿用 Python 数据层、隔离 public_worker、FrozenStockInputs 与现有七节点图。补充材料只获取一次，所有角色共享；不增加依赖、不更换模型、不迁移账号。

## 实施步骤

- [x] 修复公共接口。`vr/astock.py` 的解禁数量改用 CURRENT_FREE_SHARES、ABLE_FREE_SHARES，万股转股，未知值保留空值；历史记录按基准日截断。公告支持页码；行业强弱复用已有自动分页路径，检查去重与完整性。保留现有接口兼容性。
- [x] 新增 `duanxian/deepdive/evidence.py`，提供长期技术、板块、资金、风险四组补充材料。长期行情使用三年前复权日线，计算60/120/250日收益与均线；板块关联个股归属，提供涨跌家数及当日/5日/10日资金窗口；资金提供当日快照、融资融券、大宗交易；风险提供解禁、过去一年有界分页公告、财务摘要。所有材料标明来源、时点、单位与截断范围。
- [x] 历史120日资金流不冒充可用，公告标题不冒充全文，当前前复权不用于历史回测，资金统计不推断真实意图。数据源空返回与失败均显式说明；保留零和负数，拒绝非有限数值。可选资料失败不抹掉其他材料。
- [x] 将四个 getter 注册至 `public_worker.py`、`review_agent/deepdive.py`，原始材料写入任务证据。`graph.py`、`state.py`、`agents.py` 共享材料；`store.py` 将补充资料的机器生成缺失提示带入现有报告字段，避免模型省略。维持原有数值校验和旧报告保护。
- [x] 回归测试覆盖字段单位、公告翻页、板块全量、历史截断、空值/零/负数、失败隔离、七角色共享、冻结输入与报告提示。隔离容器执行全量 Python 测试。
- [x] 构建候选镜像，真实取数验证四组材料及隔离工作进程；确认无活动任务后保留旧镜像并更新 `.sharing/compose.json` 的 gateway。核对健康、日志及运行文件指纹，不调用用户模型、不生成报告。

## 边界

本次补充现有来源能覆盖的证据，不承诺所有风险可判断。公告最多4页、每页50条，展示近期及风险关键词样本并明示覆盖范围；未来90日解禁查询无记录仅代表接口未查到。行业数据以源返回分类为准，不把混合分类称为独立行业。当前数据不能作为过去时点已知材料。

## 实施结果

- 全量测试：1343 passed，1 skipped；前端 TypeScript/Vite 构建通过。
- 候选镜像的四个隔离取数接口均返回有效资料并冻结；七角色共享与失败隔离由回归测试覆盖。
- 浏览器检查了实际解禁空结果文案、深浅主题及页面脚本错误（无）。
- 已更新 localhost:8910 共享 gateway，健康状态 healthy；后端文件指纹与前端新产物已核对，部署后四组公开资料复测成功。
- 保留 before-evidence-expansion-20260910 旧镜像与所有原报告；未调用用户模型，用户自行触发新分析。
