# 历史资金与公告核验修复计划

目标：恢复历史资金真实请求，区分失败和无记录；核验公告正文；纠正笼统未知、首轮辩论和激励/减持混淆。

- [x] vr/astock.py：资金严格错误模式、缺值保留；公告固定源分页与身份校验。
- [x] evidence.py：账号目录保存成功资金快照，失败使用14日内缓存并标日期；最多2篇公告各6页，显式正文节选范围。
- [x] agents.py：区分已知状态与未来不确定性；首轮正方直接陈述；分开股权激励和减持。
- [x] 个股资金API保留失败状态；前端缺值和不足20条不产生虚假合计。
- [x] 执行回归测试与前端构建；真实接口验证，不调用模型。
- [x] 更新 localhost:8910 共享部署，检查健康和实际代码版本；分析由用户触发。

验证：tests/test_deepdive_recovery.py 覆盖成功/缓存/过期/未知数值、公告分页和身份、部分失败隔离、首轮提示。运行项目全量Python测试。已有未提交修改保留。

结果：完整测试1348通过、1跳过；最终公告优先级微调后相关16项再次通过。TypeScript/Vite构建通过。实际归属公告4/4页读取成功，正文超过6000字则明确节选截断。候选页面明暗主题均显示真实资金请求失败状态。生产镜像a41950caa27e，健康正常；临时账号上下文验证生产隔离取数并冻结资金/公告成功，2篇正文完整读取。历史资金上游本次仍ConnectionError且无缓存，未声称数据补齐。保留before-recovery-20260910镜像；没有触发模型或覆盖旧报告。
